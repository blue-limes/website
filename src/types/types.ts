export interface Person {
  name: string;
  role: string;
  presentation: string;
}

export interface Software {
  name: string;
  role: string;
  icon: string;
}

export interface Feature {
  name: string;
  description: string;
}
